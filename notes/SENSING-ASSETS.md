# Real Time Sensing: asset map

All six go in `assets/sensing/`. Named to match the page, so no edits needed
if you use these filenames.

| File | Which upload | Used for |
|---|---|---|
| `hero-rig.jpg` | the wide shot of the Jetson + breadboard on the wooden desk | Hero, 21:9. Crops to a wide band, so pick the one with the most horizontal spread |
| `system-diagram.png` | the black-and-white flow diagram (six inputs, three outputs) | The system section. Rendered `contain` on a dark panel, so a transparent PNG looks best |
| `seed-award.jpg` | the gold SEED Chairman Award trophy | The award section, portrait 3:4 |
| `01-breadboard.jpg` | the top-down breadboard shot with the GPS antenna and keyboard visible | Update 01, portrait 3:4 |
| `03-street-detection.jpg` | the street frame with YOLO boxes on the truck and motorcycle | Update 03, 16:9 |
| `05-rig.jpg` | whichever rig photo shows the most sensors attached | Update 05, portrait 3:4 |

Two of your uploads are **not** used yet:

- the dB rolling-median chart (acute spike vs exposure story)
- the audio classifier waveform UI with the class chips

Both are from the UIL deck rather than the LinkedIn posts, which is why they are
left out. If they are yours to publish, the audio classifier one belongs in the
"Where it is now" section next to the audio fine-tuning card, and the dB chart
belongs wherever the exposure argument goes.

## Not included, deliberately

Everything else in the PDF: the KSPCB and WHO threshold table, the school-gate
section diagram, the child-exposure framing, the privacy-by-design panel. That
is UIL work product, not your LinkedIn writing. Confirm you can republish it
before any of it goes on a personal site.
